box: debian
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
deploy:
  steps:
    - git-push:
         gh_oauth: $GIT_TOKEN
         gh_pages: true
         gh_pages_domain: blog.sergiusens.org
         basedir: public
         repo: sergiusens/sergiusens.github.com
